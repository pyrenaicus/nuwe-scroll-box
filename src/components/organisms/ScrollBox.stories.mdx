import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import ScrollBox from "./ScrollBox";

<Meta
  title="Design System/Organisms/ScrollBox"
  component={ScrollBox}
  argTypes={{
    cards: {
      table: {
        type: {
          summary: "(CardType & { id: string })[]",
          detail:
            "[{\n  id: string,\n  title: string,\n  desc: string,\n  tags: [{title: string}, {title: string}, ...]\n}]",
        },
      },
    },
  }}
/>

# ScrollBox

## a component representing a group of cards

`ScrollBox` is used when creating a list of cards, each one representing a group of gifts. **This is the component you will use**.

### Required props

1. `scrollBoxTitle`, a string representing the title of a `ScrollBox` component.

2. `loading` a boolean representing the loading state of component.

3. `cards` an array of cards, its type being an intersection of `CardType` and `{ id: string }` as React requires a unique `key` prop when rendering a list of components. A card has this shape:

```javascript
[{
  id: string,
  title: string,
  desc: string,
  tags: [ { title: string}, { title: string}, ...],
  }]
```

## `ScrollBox` component can be in three different states:

export const Template = (args) => <ScrollBox {...args} />;

### Default state

This is the default state when gift cards exist.

<Canvas>
  <Story
    name="default"
    args={{
      loading: false,
      scrollBoxTitle: "My custom horizontal scroll box",
      cards: [
        {
          id: "001",
          title: "Crew 🎉",
          desc: "Lista de regalos para el grupo crew 🎉",
          tags: [{ title: "Deportes de riesgo" }, { title: "Libros" }],
          handleClick: () => {
            console.log("clicked!");
          },
        },
        {
          id: "002",
          title: "Family 🎉",
          desc: "Lista de regalos para el grupo crew 🎉",
          tags: [{ title: "Family" }, { title: "Sport" }],
          handleClick: () => {
            console.log("clicked!");
          },
        },
        {
          id: "003",
          title: "Other 🎉",
          desc: "Lista de regalos para el grupo crew 🎉",
          tags: [{ title: "Experience" }, { title: "Libros" }],
          handleClick: () => {
            console.log("clicked!");
          },
        },
        {
          id: "004",
          title: "Crew 🎉",
          desc: "Lista de regalos para el grupo crew 🎉",
          tags: [{ title: "Deportes de riesgo" }, { title: "Libros" }],
          handleClick: () => {
            console.log("clicked!");
          },
        },
        {
          id: "005",
          title: "Family 🎉",
          desc: "Lista de regalos para el grupo crew 🎉",
          tags: [{ title: "Family" }, { title: "Sport" }],
          handleClick: () => {
            console.log("clicked!");
          },
        },
      ],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

#### Arguments of the component

<ArgsTable story="default" />

### Loading state

Showing a skeleton while cards are loading from an external API.

<Canvas>
  <Story
    name="loading"
    args={{
      loading: true,
      scrollBoxTitle: "loading gifts...",
      cards: [],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Empty state

Showing when something went wrong and there are no gift cards.

<Canvas>
  <Story
    name="empty"
    args={{
      loading: false,
      scrollBoxTitle: "No gifts found!",
      cards: [],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
