import {
  ArgsTable,
  Canvas,
  Meta,
  Story,
  ColorPalette,
  ColorItem,
} from "@storybook/addon-docs";
import Card from "./Card";

<Meta
  title="Design System/Molecules/Card"
  component={Card}
  argTypes={{
    title: {
      table: {
        type: {
          summary: "String",
          detail: "Keep it short",
        },
      },
    },
    desc: {
      table: {
        type: {
          summary: "String",
          detail: "Keep it short",
        },
      },
    },
    tags: {
      table: {
        type: {
          summary: "Tag[]",
          detail: "[{ title: string }, { title: string }, ...]",
        },
      },
    },
    handleClick: {
      table: {
        type: {
          summary: "() => void",
          detail: "in our example: () => { console.log('clicked!')}",
        },
      },
    },
  }}
/>

# Card

## A component representing a single card

`Card` is used when creating a single card, to show a group of gifts, **you don't need to use this component directly**, as cards are passed by props to a `ScrollBox`component.

### Required props

- `tags`, an array of tags with shape `[{ title: string }, { title: string }, ...]` should always be present when using `Card`.

- `title`, a string, should always be present when using `Card`.

- `desc`, a string, should always be present when using `Card`.

export const Template = (args) => <Card {...args} />;

## Colors

Using colors from [Reasonable Colors](https://reasonable.work/colors/) color system.

<ColorPalette>
  <ColorItem
    title="Tag pill background color"
    subtitle="Green shade 4 from Reasonable Colors"
    colors={{ "--color-green-4": "#008217" }}
  />
  <ColorItem
    title="Card on hover color"
    subtitle="Violet shade 2 from Reasonable Colors"
    colors={{ "--color-violet-2": "#E8DAFF" }}
  />
</ColorPalette>

### Preview

<Canvas>
  <Story
    name="Card"
    args={{
      title: "Crew 🎉",
      desc: "Lista de regalos para el grupo crew 🎉",
      tags: [
        { title: "Deportes de riesgo" },
        { title: "Libros" },
        { title: "Family" },
        { title: "Deportes de riesgo" },
      ],
      handleClick: () => {
        console.log("clicked!");
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Arguments of a Card component

<ArgsTable story="Card" />

## Testing a card with a long title and longer description

<Canvas>
  <Story
    name="Long text card"
    args={{
      title: "This is an extremely long title for a card",
      desc: "What an awesome big pack of gifts, just for your interest, let's show a detailed list of each one of them: We have amazing packs of extreme sports in the Pyrenees, and amazing restaurants to go for dinner, bla bla bla",
      tags: [
        { title: "Extreme SPORTS are cool" },
        { title: "Books" },
        { title: "Movies" },
        { title: "More extreme sports" },
        { title: "Restaurants" },
      ],
      handleClick: () => {
        console.log("clicked!");
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Arguments of an awkward Card component

<ArgsTable story="Long text card" />
